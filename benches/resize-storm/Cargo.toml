[package]
name = "agentmux-bench-resize-storm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
agentmux-bench-support = { path = "../support" }
serde = { version = "1", features = ["derive"] }
