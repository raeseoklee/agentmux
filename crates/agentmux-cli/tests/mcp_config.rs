// Phase 3 integration will register this module in the CLI. Until then, this
// harness keeps the sidecar implementation and its focused tests executable.
#![allow(dead_code)]

#[path = "../src/mcp_config.rs"]
mod mcp_config;
