# Integration Tests

Platform integration tests live here. Windows ConPTY, WSL direct, and durable WSL tests should be separated so CI can run the available subset and local lab machines can run the full matrix.
